<?xml version="1.0" encoding="utf-8"?>
<!-- بيان تطبيق REMI8 - محرك الألعاب ثنائي الأبعاد -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- الصلاحيات المطلوبة -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.REMI8"
        android:hardwareAccelerated="true"
        tools:targetApi="31">

        <!-- الشاشة الرئيسية - محرر الألعاب -->
        <activity
            android:name=".editor.EditorActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- شاشة تشغيل اللعبة -->
        <activity
            android:name=".engine.core.GameActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="sensor"
            android:theme="@style/Theme.REMI8.Fullscreen" />

        <!-- نافذة تصدير APK -->
        <activity
            android:name=".export.ExportActivity"
            android:exported="false"
            android:label="@string/export_game" />

        <!-- خدمة الخلفية للتجميع -->
        <service
            android:name=".remiscript.interpreter.CompilerService"
            android:exported="false" />

    </application>

</manifest>
